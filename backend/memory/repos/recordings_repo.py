"""Repositório da tabela ``recordings``. Stub — implementação completa em M5."""

# TODO(M5): create(game_id, fps), end(recording_id, frame_count),
# get(id), list_all(game_id=None), list_by_game(game_id), delete(id).
