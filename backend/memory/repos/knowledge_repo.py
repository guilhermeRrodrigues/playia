"""Repositório de ``knowledge`` (fatos semânticos). Stub — implementação em M8."""

# TODO(M8): create(Knowledge), search_by_embedding(game_id, query, k) via
# vec_knowledge MATCH, list_by_game(game_id).
