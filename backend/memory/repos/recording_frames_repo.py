"""Repositório de ``recording_frames``. Stub — implementação completa em M5."""

# TODO(M5): insert_many(frames: Iterable[RecordingFrame]) — batch insert,
# performance crítica (~30 inserts/s); list_by_recording(recording_id);
# count_by_recording(recording_id).
