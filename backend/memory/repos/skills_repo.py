"""Repositório de ``skills`` (curated). Stub — implementação completa em M8."""

# TODO(M8): create(Skill), update_stats(id, success), search_by_embedding(
# game_id, query_embedding, k) via vec_skills MATCH, list_by_game(game_id).
